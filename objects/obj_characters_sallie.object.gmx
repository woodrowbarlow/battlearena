<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>spr_characters_sallie_idle</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>-1</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>/// Initialize Variables

player_id = -1; // must always be overridden in arena's creation code
character_health = 100;
character_lives = 5;
jump_height = 1;
move_speed = 1;
arena_gravity = 1; // overridden by arena's creation code if gravity is abnormal
delta_x = 0;
delta_y = 0;
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>// read input from controller/keyboard
var move_horiz = scr_pressed_right(player_id, true) - scr_pressed_left(player_id, true);
var move_vert = scr_pressed_down(player_id, true) - scr_pressed_up(player_id, true);
var jump = scr_pressed_jump(player_id, false);

// if we're standing on solid ground
if(place_meeting(x,y+1,obj_plat_par_solid)
    || (move_vert &lt;= 0 &amp;&amp; place_meeting(x,y+1,obj_plat_par_semisolid) &amp;&amp;
    !place_meeting(x,y,obj_plat_par_semisolid))) {
    // move_speed is a signed float from -1 to 1 indicating magnitude and direction
    delta_x = 5 * move_speed * move_horiz;
    delta_y = 0;
    // if jump was pressed since last frame
    if(jump &gt; 0) {
        // for y-axis, positive is down and negative is up
        delta_y = -12 * jump_height;
    }
}

// if we're in mid-air
else {
    // you can't control movement in mid-air as well
    delta_x += 0.5 * move_speed * move_horiz;
    if(delta_x &gt; 5 * move_speed) delta_x = 5 * move_speed;
    else if(delta_x &lt; -5 * move_speed) delta_x = -5 * move_speed;
    // apply a force downward
    delta_y += arena_gravity;
    // terminal velocity check
    if(delta_y &gt; 10*arena_gravity) {
        delta_y = 10*arena_gravity;
    }
}

// if we're about to collide horizontally with a solid object
if(place_meeting(x + delta_x, y, obj_plat_par_solid)) {
    // get as close to it as we can without actually intersecting
    while(!place_meeting(x + sign(delta_x), y, obj_plat_par_solid)) {
        x += sign(delta_x);
    }
    // then stop moving horizontally
    delta_x = 0;
}

// if we're about to collide vertically with a solid object
if(place_meeting(x, y + delta_y, obj_plat_par_solid)) {
    // get as close to it as we can without actually intersecting
    while(!place_meeting(x, y + sign(delta_y), obj_plat_par_solid)) {
        y += sign(delta_y);
    }
    // then stop moving vertically
    delta_y = 0;
}
// if we're falling onto a semisolid platform
// (these are platforms that you can only collide with from above)
else if(delta_y &gt; 0 &amp;&amp;
    place_meeting(x, y + delta_y, obj_plat_par_semisolid) &amp;&amp;
    !place_meeting(x,y,obj_plat_par_semisolid)) {
    if(move_vert &gt; 0) {
        y ++;
    }
    else {
        // get as close to it as we can without actually intersecting
        while(!place_meeting(x, y+1, obj_plat_par_semisolid)) {
            y ++;
        }
        // then stop moving vertically
        delta_y = 0;
    }
}

// update our coordinates
x += delta_x;
y += delta_y;
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
